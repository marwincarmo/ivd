% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{.ivd_priors}
\alias{.ivd_priors}
\title{Resolve the prior specification for ivd()}
\usage{
.ivd_priors(priors, mean_pred, sd_pred)
}
\arguments{
\item{priors}{Named list with any of \code{beta_intercept}, \code{beta}, \code{zeta}
(each \code{c(mean = , sd = )}), \code{sigma_rand} (\code{c(df = , scale = )}),
\code{lkj_eta} (single positive number), and \code{nu} (\code{c(shape = , rate = )},
student-t df prior). Partial specifications are filled with the
defaults.}

\item{mean_pred, sd_pred}{Empirical mean and SD of the response, used for
the default location-intercept prior.}
}
\value{
Fully resolved named list of the same shape, with an
\code{empirical_intercept} attribute recording whether the intercept prior
kept its data-dependent default.
}
\description{
Resolve and validate the \code{priors} argument of \code{\link[=ivd]{ivd()}}
}
\details{
Fills a partial user specification with the package defaults and
validates it. Only the hyperparameters are tunable -- the families are
fixed (normal for fixed effects, half-t for random-effect SDs, LKJ for
the correlation): this keeps the NIMBLE model code static so that a
user's \code{priors} can never inject model code.
}
\author{
Philippe Rast
}
\keyword{internal}
