% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_diagnostics.R
\name{pip_diagnostics}
\alias{pip_diagnostics}
\title{Monte Carlo error and chain agreement of the PIPs}
\usage{
pip_diagnostics(object, pip_level = 0.75)
}
\arguments{
\item{object}{An object of class \code{ivd}.}

\item{pip_level}{PIP threshold whose classification is checked for
between-chain agreement. Defaults to 0.75, matching \code{\link[=plot.ivd]{plot.ivd()}}.}
}
\value{
A data frame of class \code{pip_diagnostics}: one row per cluster x
random scale effect with \code{scale_var}, \code{cluster_index}, \code{cluster_id},
\code{pip}, the per-chain PIP columns, \code{mcse}, \code{range}, and \code{unstable}.
\code{pip_level} is stored as an attribute. Its \code{print()} method shows a
compact summary and the unstable clusters, if any.
}
\description{
Monte Carlo diagnostics for the posterior inclusion probabilities
}
\details{
A PIP is the posterior mean of a binary spike-and-slab indicator, so the
usual continuous-parameter diagnostics do not transfer: split-Rhat on a
0/1 chain is misleading, and a seemingly precise PIP can still differ
markedly between chains when the indicator mixes slowly. This function
reports, per cluster and random scale effect:
\itemize{
\item the per-chain PIPs (columns \code{chain1}, \code{chain2}, ...),
\item \code{mcse}: the Monte Carlo standard error of the pooled PIP,
estimated from the between-chain spread
(\eqn{\mathrm{sd}(\text{chain PIPs})/\sqrt{C}}),
\item \code{range}: the largest between-chain difference, and
\item \code{unstable}: whether the chains disagree on the classification at
\code{pip_level} (some chains put the cluster at or above the
threshold, others below it).
}
Clusters flagged \code{unstable} should not be classified either way from
this fit -- run more iterations (or more chains) first.
}
\examples{
\dontrun{
diag <- pip_diagnostics(fit)
diag
subset(diag, unstable)
}
}
\author{
Philippe Rast
}
