% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_ivd.R
\name{simulate_ivd}
\alias{simulate_ivd}
\title{Simulate data with deviant within-cluster variances}
\usage{
simulate_ivd(
  J = 100,
  n_j = 20,
  n_deviant = ceiling(0.05 * J),
  deviant_clusters = NULL,
  deviant_factor = 2,
  beta = 0,
  sigma_within = 1,
  tau_loc = 0.5,
  seed = NULL
)
}
\arguments{
\item{J}{Number of clusters. Defaults to 100.}

\item{n_j}{Observations per cluster: a single number or a length-\code{J}
vector. Defaults to 20.}

\item{n_deviant}{Number of deviant clusters, drawn at random. Defaults
to 5\% of \code{J} (rounded up). Ignored when \code{deviant_clusters} is given.}

\item{deviant_clusters}{Optional integer vector of cluster indices to
make deviant (overrides \code{n_deviant}).}

\item{deviant_factor}{Multiplicative factor on the within-cluster SD of
the deviant clusters: 2 doubles it, 0.5 halves it. A single number or
one value per deviant cluster. Defaults to 2.}

\item{beta}{Location grand mean (fixed intercept). Defaults to 0.}

\item{sigma_within}{Baseline within-cluster SD of the regular clusters.
Defaults to 1.}

\item{tau_loc}{SD of the location random intercepts. Defaults to 0.5.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A list of class \code{ivd_sim}:
\itemize{
\item \code{data}: data frame with \code{y} and cluster \code{id}, ready for
\code{\link[=ivd]{ivd()}}.
\item \code{truth}: data frame with one row per cluster: \code{id},
\code{u_loc}, \code{sd_within}, \code{factor}, and \code{deviant}.
\item \code{params}: the simulation parameters.
}
}
\description{
Simulate MELSM data with known deviant clusters
}
\details{
Generates data from the intercept-only mixed-effects location-scale
model that \code{\link[=ivd]{ivd()}} fits, with a known subset of clusters whose residual
(within-cluster) SD deviates from the common baseline:
\deqn{y_{ij} = \beta_0 + u_j + e_{ij}, \quad
      e_{ij} \sim N(0, \sigma_j^2),}
where \eqn{u_j \sim N(0, \tau_{loc}^2)} are location random intercepts
and \eqn{\sigma_j = \sigma_{within} \cdot f_j} with \eqn{f_j = 1} for
regular clusters and \eqn{f_j =} \code{deviant_factor} for the deviant ones.

Because the ground truth is known, the output is suited for power
analysis (how many clusters/observations to detect a given SD
inflation?), teaching, and validating recovery: fit with
\code{ivd(y ~ 1 + (1 | id), ~ 1 + (1 | id), data = sim$data, ...)} and
compare \code{pip(fit)} against \code{sim$truth}.
}
\examples{
sim <- simulate_ivd(J = 20, n_j = 10, n_deviant = 2, seed = 1)
sim
subset(sim$truth, deviant)
\dontrun{
fit <- ivd(y ~ 1 + (1 | id), ~ 1 + (1 | id), data = sim$data,
           niter = 2000, nburnin = 2000)
merge(pip(fit), sim$truth, by.x = "cluster_id", by.y = "id")
}
}
\author{
Philippe Rast
}
