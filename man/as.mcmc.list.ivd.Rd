% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridges.R
\name{as.mcmc.list.ivd}
\alias{as.mcmc.list.ivd}
\title{Convert an ivd fit to an mcmc.list}
\usage{
\method{as.mcmc.list}{ivd}(x, ...)
}
\arguments{
\item{x}{An object of class \code{ivd}.}

\item{...}{Not used.}
}
\value{
A \code{\link[coda:mcmc.list]{coda::mcmc.list()}} with renamed parameter columns.
}
\description{
Convert a fitted ivd model to a coda \code{mcmc.list}
}
\details{
Returns the posterior samples as a \code{\link[coda:mcmc.list]{coda::mcmc.list()}} with one element
per chain, restricted to the parameters shown by \code{summary()} and renamed
to the same human-readable labels (\code{Intc}, \verb{scl_*}, \verb{sd_*}, \code{R[...]},
\code{pip[...]}). This makes a fit directly usable with the wider MCMC
ecosystem, e.g. \code{bayesplot::mcmc_trace()} or any coda function.
}
\examples{
\dontrun{
post <- as.mcmc.list(fit)
coda::effectiveSize(post)
bayesplot::mcmc_trace(post, pars = "Intc")
}
}
\author{
Philippe Rast
}
