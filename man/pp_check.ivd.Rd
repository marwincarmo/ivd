% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_check.R
\name{pp_check.ivd}
\alias{pp_check.ivd}
\title{Posterior predictive check for ivd models}
\usage{
\method{pp_check}{ivd}(
  object,
  ndraws = 100,
  type = c("intervals", "density"),
  probs = c(0.5, 0.9),
  labels = c("index", "original"),
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An object of class \code{ivd}.}

\item{ndraws}{Number of posterior draws to replicate data from.
Defaults to 100 (capped at the number of stored draws).}

\item{type}{\code{"intervals"} (default) or \code{"density"}; see Details.}

\item{probs}{Inner and outer probability of the predictive intervals
for \code{type = "intervals"}. Defaults to \code{c(0.5, 0.9)}.}

\item{labels}{Cluster labels for the highlighted points: \code{"index"}
(default) uses the internal 1..J index; \code{"original"} the user's own
grouping IDs. Matches \code{\link[=summary.ivd]{summary.ivd()}} and \code{\link[=plot.ivd]{plot.ivd()}}.}

\item{seed}{Optional seed making the replicate noise reproducible.}

\item{...}{Not used.}
}
\value{
A \code{ggplot} object.
}
\description{
Posterior predictive check of the within-cluster SDs
}
\details{
The central claim of an \code{ivd} fit concerns the residual (within-cluster)
variability, so the check replicates data from the posterior and
compares each cluster's \emph{observed} within-cluster SD with its posterior
predictive distribution:
\itemize{
\item \code{type = "intervals"} (default): per cluster, the observed
SD against the inner/outer predictive intervals, ordered by
observed SD. Clusters falling outside the outer interval are
highlighted and labelled -- systematic misfit there suggests the
normal likelihood is inadequate (e.g. heavy tails).
\item \code{type = "density"}: the observed density of cluster SDs
overlaid on the replicated densities (one thin line per draw).
}
Clusters with a single observation are dropped (their SD is undefined).
}
\examples{
\dontrun{
pp_check(fit)
pp_check(fit, type = "density", ndraws = 50)
}
}
\author{
Philippe Rast
}
