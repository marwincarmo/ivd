% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_check.R
\name{.pp_cluster_sds}
\alias{.pp_cluster_sds}
\title{Observed and replicated within-cluster SDs}
\usage{
.pp_cluster_sds(object, ndraws = 100, seed = NULL)
}
\arguments{
\item{object}{An object of class \code{ivd}.}

\item{ndraws}{Number of posterior draws to simulate from (sampled
without replacement from the pooled chains; capped at the number of
available draws).}

\item{seed}{Optional seed for reproducibility of the draw sampling and
the replicate noise.}
}
\value{
List with \code{observed} (named length-J vector of within-cluster
SDs), \code{rep} (an \code{ndraws} x J matrix of replicated SDs), and \code{n_obs}
(cluster sizes). Clusters with fewer than 2 observations are dropped
(their SD is undefined).
}
\description{
Internal core of \code{\link[=pp_check.ivd]{pp_check.ivd()}}: reconstructs \code{mu} and \code{tau}
per posterior draw from the stored design matrices, simulates replicated
data \code{Y_rep ~ N(mu, tau)}, and computes the within-cluster SD of
every replicate. Pure R (no NIMBLE), so it is unit-testable under
coverage.
}
\keyword{internal}
